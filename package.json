{
    "name": "lexifer",
    "version": "1.1.0-alpha.6",
    "private": true,
    "scripts": {
        "build": "cat src/*.ts > combined.ts && sed -i '/export/d' combined.ts && sed -i '/import/d' combined.ts && tsc",
        "macbuild": "cat src/*.ts > combined.ts && sed -i '' '/export/d' combined.ts && sed -i '' '/import/d' combined.ts && tsc"
    },
    "devDependencies": {
        "@types/node": "^16.4.6",
        "typescript": "^4.3.5"
    },
    "peerDependencies": {
        "typescript": "^4.3.5"
    }
}
